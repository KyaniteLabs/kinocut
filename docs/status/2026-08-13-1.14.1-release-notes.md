# Kinocut 1.14.1 release notes

**Published:** 2026-08-13 — PyPI `kinocut==1.14.1`, npm `kinocut@1.14.1`, GitHub Release `v1.14.1`.

**Published surface:** 196 MCP tools / 167 CLI commands (unchanged).

**Compat:** `mcp-video==1.6.10` installs `kinocut==1.14.1`.

## Three agent-facing bullets

1. **What agents can do now** — Same 1.14.0 surface: stitched equirect 360 → `360_assembly_plan` → approve → `v360` split / switch / PiP / single. No new MCP name.
2. **What got safer** — GitHub pyright now type-checks the rescue cancel path (`_handle_cancellation` is `NoReturn`). Lint on the release commit stays green.
3. **Compat** — `mcp-video==1.6.10` installs `kinocut==1.14.1`. Counts stay **196 / 167**.

## Not claimed

Same honesty as 1.14.0: not a vendor stitcher, not an optimized-AI-director, native MCPB staged/local-only.
